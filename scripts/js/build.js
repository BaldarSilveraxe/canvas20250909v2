import Konva from 'https://esm.sh/konva@9';

const setStageLayersGroupKeyItems = (() => {
        
        console.log(canvasState);
        
return {
        //create
    };
})();

export {
    board
};
